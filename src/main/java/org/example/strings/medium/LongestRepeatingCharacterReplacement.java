package org.example.strings.medium;

public class LongestRepeatingCharacterReplacement {
    /*
    Given a string s and an integer k, you can replace any character at most k times.
    Return the length of the longest substring containing the same letter you can get.
    Pattern: Sliding Window + Frequency Counting
     */
    public static void main(String[] args) {
        System.out.println(characterReplacement("AABABBA", 1)); // Output: 4
    }

    private static int characterReplacement(String s, int k) {

        int left =0, right =0, maxLen =0;

        return maxLen;
    }
}
